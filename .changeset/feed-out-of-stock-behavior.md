---
"saleor-app-products-feed": minor
---

You can now decide what happens to products with no stock available. In the new "Stock availability" section of the configuration you can either keep them in the feed marked as `out_of_stock` (this stays the default, so nothing changes unless you say so), or exclude them from the feed entirely.
