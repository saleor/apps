---
"saleor-app-onboarding": minor
---

Add secondary go-live tracks on the home Store Readiness guide: connect customer email (SMTP) and review templates, plus a Paper storefront card (deploy, API env, cache/webhooks, Saleor Models CMS). On wide screens Paper sits beside the commerce checklist; both cards use the Dashboard-style elevated shadow. These steps stay optional and do not change the required "ready to sell" progress.
