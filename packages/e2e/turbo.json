{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "env": [
        "CI",
        "INSTANCE_URL",
        "DASHBOARD_USER_EMAIL",
        "DASHBOARD_USER_PASSWORD"
      ]
    }
  }
}
