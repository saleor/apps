# Upstream source

- **Repo:** saleor-dashboard
- **Path:** `src/components/Settings/SettingsOwnershipChip.tsx` (+ `.module.css`)
- **Commit:** `1f2391dcec285d5dc067e9ca6ceb574b159f9d73`

## Intentional divergences

- Imports from `@saleor/macaw-ui` instead of `@saleor/macaw-ui-next`.
- Labels are plain English `"Shop"` / `"Channel"` (no `react-intl`).
