import { describe, it } from "vitest";

describe("DynamoConfigMapper", () => {
  it.todo("should map DynamoDB entity to AppConfig", () => {});
  it.todo("should map AppConfig to DynamoDB PutItemInput", () => {});
  it.todo("should map empty AppConfig to DynamoDB PutItemInput", () => {});
});
