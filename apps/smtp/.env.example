# ============================================================================
# MINIMAL LOCAL DEVELOPMENT SETUP
# ============================================================================
# Copy this file to .env.local and set these two values:
#
#   SECRET_KEY=any-random-string-for-local-dev
#   APL=file
#
# That's it! Run `pnpm dev` and the app will start.
# App data will be stored in a local file (no DynamoDB needed).
# ============================================================================

# Required: Key used for metadata encryption
SECRET_KEY=

# APL (App Persistence Layer) - where app installation data is stored
# Options: file (local dev), dynamodb (production), upstash, saleor-cloud
# Default: file
APL=file

# REST APL config (only if APL=saleor-cloud)
REST_APL_ENDPOINT=
REST_APL_TOKEN=

APP_LOG_LEVEL=info

# Variable controlling Saleor domains that app can be installed on.
# It's a regex pattern.
ALLOWED_DOMAIN_PATTERN=/*/

# Local development variables. When developed locally with Saleor inside docker, these can be set to:
# APP_IFRAME_BASE_URL = http://localhost:3000, so Dashboard on host can access iframe
# APP_API_BASE_URL=http://host.docker.internal:3000 - so Saleor can reach App running on host, from the container.
# If developed with tunnels, set this empty, it will fallback to default Next's localhost:3000
# https://docs.saleor.io/developer/extending/apps/local-app-development
# APP_IFRAME_BASE_URL=
# APP_API_BASE_URL=

# ============================================================================
# DYNAMODB CONFIG (only required when APL=dynamodb)
# ============================================================================
# DYNAMODB_MAIN_TABLE_NAME=
# AWS_REGION=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# DYNAMODB_REQUEST_TIMEOUT_MS=5000
# DYNAMODB_CONNECTION_TIMEOUT_MS=2000

# ============================================================================
# OPTIONAL: OTEL (OpenTelemetry) for observability
# ============================================================================
# OTEL_ENABLED=true
# OTEL_SERVICE_NAME=saleor-app-smtp
# OTEL_LOG_LEVEL=info
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_ACCESS_TOKEN=token
# NEXT_RUNTIME=nodejs

# ============================================================================
# OPTIONAL: Fallback SMTP configuration
# ============================================================================
# Allows sending emails without user configuration (user must enable in app settings)
# For testing, try tools like https://develmail.com/
# FALLBACK_SMTP_HOST=
# FALLBACK_SMTP_PORT=
# FALLBACK_SMTP_USER=
# FALLBACK_SMTP_PASSWORD=
# FALLBACK_SMTP_ENCRYPTION=NONE
# FALLBACK_SMTP_SENDER_NAME=
# FALLBACK_SMTP_SENDER_EMAIL=
