import { env } from "@/env";

export const FEED_CACHE_MAX_AGE = env.FEED_CACHE_MAX_AGE;

export const MAX_PARALLEL_CALLS = env.MAX_PARALLEL_CALLS;

export const VARIANTS_PER_PAGE = env.VARIANTS_PER_PAGE;
