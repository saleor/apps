mutation DeletePublicMetadata($id: ID!, $keys: [String!]!) {
  deleteMetadata(id: $id, keys: $keys) {
    errors {
      message
      code
    }
    item {
      metadata {
        ...MetadataItem
      }
    }
  }
}
