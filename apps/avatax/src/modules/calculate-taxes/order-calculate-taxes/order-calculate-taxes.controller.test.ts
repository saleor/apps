import { describe, it } from "vitest";

describe("OrderCalculateTaxesController", () => {
  it.todo("Validates payload and returns proper errors early");
  it.todo("Calls use case if payload is valid and returns its result with status 200");
  it.todo("Maps useCase errors to http responses");
});
