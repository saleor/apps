query TransactionDetailsViaPsp($pspReference: String!) {
  orders(first: 1, search: $pspReference) {
    edges {
      node {
        id
        number
        transactions {
          ...Transaction
        }
      }
    }
  }
}
