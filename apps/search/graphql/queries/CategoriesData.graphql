query CategoriesDataForImport($first: Int, $after: String) {
  categories(first: $first, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        ...CategoryData
      }
    }
  }
}
