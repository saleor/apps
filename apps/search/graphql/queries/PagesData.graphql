query PagesDataForImport($first: Int, $after: String, $filter: PageFilterInput) {
  pages(first: $first, after: $after, filter: $filter) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        ...PageData
      }
    }
  }
}
